var VisBase = function() {
};

// what attrObj used for?
// VisBase.prototype.animateAttrKeys = function(keys, attrObj, speed, easing) {
	// keys = _.extend(
		// {},
		// {
			// include: ['member', 'edge', 'text'],
			// exclude: []
		// },
		// keys || {}
	// );
	// var attr = this.getAttributes();
	// _.each(keys.include, function(key) {
		// attr[key] = _.extend(
			// {},
			// attr[key],
			// attrObj
		// );
	// });
// 
	// _.each(keys.exclude, function(key) {
		// delete attr[key];
	// });
// 
	// this.animateToAttr(attr, speed, easing);
// };
